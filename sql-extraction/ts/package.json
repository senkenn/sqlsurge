{
  "name": "@senken/sql-extraction-ts",
  "main": "./out/index.js",
  "scripts": {
    "prepublishOnly": "pnpm build",
    "test": "jest",
    "build": "tsc -p ."
  },
  "devDependencies": {
    "@senken/config": "workspace:*"
  }
}
