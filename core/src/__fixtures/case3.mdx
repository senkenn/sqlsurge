ignore me

```ts:foo.ts
export const x = 1;
```

```ts:run.ts
import { x } from "./case3.mdx@foo.ts";
import { test } from "node:test";
import { ok, equal } from "node:assert";

test("hello", () => {
  ok(true);
  equal(x, 1);
});
```
